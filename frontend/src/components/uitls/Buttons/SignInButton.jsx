import style from "./SignInButton.module.css";
import React from "react";

const SignInButton = () => {
  return <button className={style.buttonStyle}>Sign In</button>;
};

export default SignInButton;
