import style from "./CreateAccount.module.css";
import React from "react";
const CreateAccountButton = () => {
  return <button className={style.buttonStyle}>Create Account</button>;
};

export default CreateAccountButton;
